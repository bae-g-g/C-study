# 10. 콤마 연산자

<!-- EDUCATION-CONTEXT:START -->
<p align="right">
  <a href="../README.md">전체 목차</a> · <a href="./README.md">단원 목차</a>
</p>

## 학습 안내

> 단원: [02. 입출력 및 연산자](./README.md)  
> 이전 학습: [09. 비트 연산자](./09_비트_연산자.md)  
> 다음 학습: [11. 조건 연산자](./11_조건_연산자.md)

### 학습 목표

- 콤마 연산자의 핵심 개념과 사용 목적을 설명할 수 있다.
- 예제 코드를 읽고 실행 흐름과 결과를 단계별로 추적할 수 있다.
- 이전 단원에서 배운 개념과 다음 단원에서 이어질 내용을 연결할 수 있다.

### 수업 흐름

1. Comma Operator 쉼표 연산자
<!-- EDUCATION-CONTEXT:END -->

## **Comma Operator 쉼표 연산자**

**com\_1.c**

```c
#include <stdio.h>

int main()
{
    int X, Y, Z;

    // Comma가 Parameter 구별로 사용될 때
    scanf("%d %d %d", &X, &Y, &Z);  // %d 정수자료형 1, 2, 3(4 Bytes) X, Y, Z 주소값(&)을 시작으로 입력/저장
                                    // Window OS
                                    // 1) &Z, &Y, &X 순서(R→L)로 각각의 변수 주소(&)에 메모리박스(+TEMP 생성) 마련 
                                    // 2) "%d %d %d" 순서(L→R)로 변수 입력/저장
                                    // 3) scanf 실행(각각의 주소에 변수 입력/저장된 것 읽고 실행 )
    printf("%d\n", X);
    printf("%d\n", Y);
    printf("%d\n", Z);

    // Comma가 Code 구별로 사용될 때
    X = 10, Y = 20, Z = 30, printf("%d\n", X), printf("%d\n", Y), printf("%d\n", Z); // 순서(L→R)  ∵ Code 구별 

    // Comma가 Code 구별로 사용될 때
    // , = "; + \n"
    X = 10;
    Y = 20;
    Z = 30;
    printf("%d\n", X);
    printf("%d\n", Y);
    printf("%d\n", Z);

    return 0;
}
```

**com\_2.c**

```c
#include <stdio.h>

int main()
{
    // Comma가 Parameter 구별로 사용될 때
    int A;
    char B;
    scanf("%d %d", &A, &B); // %d 정수자료형 3, 89(4 Bytes) A, B 주소값(&)을 시작으로 입력/저장
                            // Window OS
                            // 1) &B, &A 순서(R→L)로 각각의 변수 주소(&)에 TEMP 생성/마련
                            // 2) "%d %d" 변수 입력/저장: "%d %d" 순서(L→R)로 각각 위치에 Match되는 A, B 변수 입력/저장 (3 → A,  89 → B)
                            // 3) scanf 실행(각각의 주소에 변수 입력/저장된 것 읽고 실행)
    printf("%d %d\n", A, B);

    printf("-------");

    int C;
    char D;
    scanf("%d %d", &D, &C); // %d 정수자료형 3, 89(4 Bytes) C, D 주소값(&)을 시작으로 입력/저장
                            // Window OS
                            // 1) &C, &D 순서(R→L)로 각각의 변수 주소(&)에 TEMP 생성/마련
                            // 2) "%d %d" 변수 입력/저장: "%d %d" 순서(L→R)로 각각 위치에 Match되는 D, C 변수 입력/저장 (3 → D,  89 → C)
                            // 3) scanf 실행(각각의 주소에 변수 입력/저장된 것 읽고 실행)
    printf("%d %d\n", C, D);    
    
    // Comma가 Code 구별로 사용될 때
    // , = "; + \n"
    int X = 1;
    int Y = 11;

    printf("%d\n", X), printf("%d\n", Y), X = 100, printf("%d\n", X);

    return 0;
}
```

---
<!-- LESSON-NAV:START -->
[이전: 09. 비트 연산자](./09_비트_연산자.md) · [단원 목차](./README.md) · [다음: 11. 조건 연산자](./11_조건_연산자.md)
<!-- LESSON-NAV:END -->

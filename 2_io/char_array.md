# Char vs Array

# **char array**

`scanf %c`
→ 1 Byte 크기 문자 1개(%c)를 메모리박스의 주소값(&)에 입력/저장
※ 문자열 마지막 \\0=null 문자 생성 X
※ 배열선언시 \\0=null Set-Up 필요 O ∵ printf %s로 출력하기 위함 (\\0=null 전 까지 출력)
※ 공백문자(엔터, 스페이스) 문자인식 O

`scanf %s`
→ 1 Byte 크기 문자 여러개(%s)를 메모리박스의 주소값(&)에 입력/저장
※ 문자열 마지막 \\0=null 문자 생성 O
※ 배열선언시 \\0=null Set-Up 필요 X
※ 공백문자(엔터, 스페이스) 문자인식 X

`printf %c`
→ 선언한 자료형크기 만큼 문자(%c) 1개 형식(형태 + 크기) 출력
※ 배열선언시 문자'열' 출력 위해 사용 X
e.g.)
int X\[7\];
printf("%c", X); → 출력오류
※ 배열선언시 문자 1개 출력 위해 사용 O
e.g.)
int X\[7\];
printf("%c", X\[0\]); → 출력가능

`printf %s`
→ /0=null 문자 전까지 문자열 출력 ∵ printf %s 고유 성질
※ 배열선언시 문자'열' 출력 위해 사용 O

### **Example)**

**f\_char\_array\_1.c**

```cpp
// 배열크기 O  /  공백문자 X  /  문자 → %c  문자'열 → %s
// 예:  A 입력/저장 → BCDEFG 입력/저장 → HIJKLMN 입력/저장

#include <stdio.h>

int main()
{
    char X;
    printf("---Input---\n");
    scanf("%c", &X);           // 입력서식(%c→문자) '초기화' 변수자료형(문자→A엔터) 매칭 O
                               // scanf 실행 성공:  문자 1개 A 입력/저장[%c 문자자료형 A(1 Byte) A 주소값(&)에 입력/저장]  →  '초기화' 변수(엔터) 버퍼 유지
    printf("---Output---\n");
    printf("%c\n", X);         // %c → 선언한 자료형크기 만큼 문자 1개 A 출력
    
    char Y[7];
    printf("---Input---\n");
    scanf("%s", &Y);           // scanf 실행:  버퍼 변수(문자→엔터) 삭제  →  입력서식(%s→문자'열') '초기화' 변수자료형(문자'열'→BCDEFG엔터) 매칭 O
                               // scanf 실행 성공:  Y 주소값(&)에 문자 6개 BCDEFG 입력/저장(공백문자 전까지) + 문자 1개 \0=null 생성  → '초기화' 변수(문자→엔터) 버퍼 유지 
    printf("---Output---\n");
    printf("%s\n", Y);         // %s → 문자 6개 BCDEFG 출력:  \0=null 문자 전까지 출력
    printf("%d\n", strlen(Y)); // %d → 문자'열' 길이(strlen = string length) 출력  → \0=null 문자 앞까지 길이 출력
    printf("<%c>\n", Y[6]);    // \0=null 문자 생성 위치 

    char Z[8];
    printf("---Input---\n");
    scanf("%s", &Z);          // scanf 실행:  버퍼 변수(문자→엔터) 삭제  →  입력서식(%s→문자'열') '초기화' 변수자료형(문자'열'→HIJKLMN엔터) 매칭 O
                              // scanf 실행 성공:  Z 주소값(&)에 문자 7개 HIJKLMN 입력/저장(공백문자 전까지) + 문자 1개 \0=null 생성  → '초기화' 변수(문자→엔터) 버퍼 유지
    printf("---Output---\n");
    printf("%s\n", Z);        // %s → 문자 7개 HIJKLMN 출력:  \0=null 문자 전까지 출력
    printf("%d\n", strlen(Z)); // %d → 문자'열' 길이(strlen = string length) 출력  → \0=null 문자 전까지 길이 출력
    printf("<%c>\n", Z[7]);    // \0=null 문자 생성 위치 

    return 0;
}


```

![](https://t9003081320.p.clickup-attachments.com/t9003081320/9a6a6913-d773-46e6-b37b-73f629562df2/Picture7.png)

**f\_char\_array\_2.c**

```cpp
// 공백문자 O  /  문자 → %c
// 예:  스페이스스페이스J스페이스 입력/저장

#include <stdio.h>

int main()
{
    char I;

    scanf("%c", &I);     // 입력서식(%c→문자) '초기화' 변수자료형(문자→스페이스스페이스J스페이스엔터) 매칭 O
                         // scanf 실행 성공:  문자 1개 스페이스 입력/저장[%c 문자자료형 스페이스(1 Byte) I 주소값(&)에 입력/저장]  →  '초기화' 변수(문자→스페이스J스페이스엔터) 버퍼 유지
    printf("<%c>\n", I); // %c → 선언한 자료형크기 만큼 문자 1개 스페이스 출력

    scanf("%c", &I);     // 입력서식(%c→문자) 버퍼 변수자료형(문자→스페이스J스페이스엔터) 매칭 O
                         // scanf 실행 성공:  문자 1개 스페이스 입력/저장[%c 문자자료형 스페이스(1 Byte) I 주소값(&)에 입력/저장]  →  버퍼 변수(문자→J스페이스엔터) 유지
    printf("<%c>\n", I); // %c → 선언한 자료형크기 만큼 문자 1개 스페이스 출력

    scanf("%c", &I);     // 입력서식(%c→문자) 버퍼 변수자료형(문자→J스페이스엔터) 매칭 O
                         // scanf 실행 성공:  문자 1개 J 입력/저장[%c 문자자료형 J(1 Byte) I 주소값(&)에 입력/저장]  →  버퍼 변수(문자→스페이스엔터) 유지
    printf("<%c>\n", I); // %c → 선언한 자료형크기 만큼 문자 1개 J 출력

    scanf("%c", &I);     // 입력서식(%c→문자) 버퍼 변수자료형(문자→스페이스엔터) 매칭 O
                         // scanf 실행 성공:  문자 1개 스페이스 입력/저장[%c 문자자료형 스페이스(1 Byte) I 주소값(&)에 입력/저장]  →  버퍼 변수(문자→엔터) 유지
    printf("<%c>\n", I); // %c → 선언한 자료형크기 만큼 문자 1개 스페이스 출력

    scanf("%c", &I);     // 입력서식(%c→문자) 버퍼 변수자료형(문자→엔터) 매칭 O
                         // scanf 실행 성공:  문자 1개 엔터 입력/저장[%c 문자자료형 엔터(1 Byte) I 주소값(&)에 입력/저장]
    printf("<%c>\n", I); // %c → 선언한 자료형크기 만큼 문자 1개 엔터 출력

    return 0;
}


```

![](https://t9003081320.p.clickup-attachments.com/t9003081320/9fe21b2e-6c36-4503-b9e2-8f3b0a93cd87/Picture22.png)

**f\_char\_array\_3.c**

```cpp
// 배열크기 O  /  공백문자 O  /  문자 → %s
// 예:  스페이스스페이스JK스페이스스페이스LMN 입력/저장 

#include <stdio.h>

int main()
{
    char I[10] = {0};

    printf("---Input---\n");
    scanf("%s", &I);         // 입력서식(%s→문자'열') '초기화' 변수자료형(문자→스페이스스페이스JK스페이스스페이스LMN엔터) 매칭 O → 공백문자 예외문자 Set UP 
                             // scanf 실행 대기:  문자 1개 스페이스 입력/저장 X 버퍼유지 O  →  버퍼 변수(문자→스페이스) 삭제  →  '초기화' 변수(문자→스페이스JK스페이스스페이스LMN엔터) 버퍼 유지 
                             // scanf 실행 대기:  문자 1개 스페이스 입력/저장 X 버퍼유지 O  →  버퍼 변수(문자→스페이스) 삭제  →  버퍼 변수(문자→JK스페이스스페이스LMN엔터) 유지
                             // scanf 실행: 입력서식(%s→문자'열') 버퍼 변수자료형(문자'열'→JK스페이스스페이스LMN엔터) 매칭 O
                             // scanf 실행 성공:  I 주소값(&)에 문자 2개 JK 입력/저장(공백문자 전까지) + 문자 1개 \0=null 생성  →  버퍼 변수(문자→스페이스스페이스LMN엔터)유지
    printf("---Output---\n"); 
    printf("<%s>\n", I);     // %s → 문자 2개 JK 출력
    printf("<%c> <%c>\n", I[0], I[1]); // %c → 선언한 자료형크기 만큼 문자 1개 J K 각각 출력
    printf("<%c>\n", I[2] ); // \0=null 문자 생성 위치 

    printf("---Input---\n");
    scanf("%s", &I);         // scanf 실행:  문자 1개 스페이스 입력/저장 X 버퍼유지 O →  버퍼 변수(문자→스페이스) 삭제  →  버퍼 변수(문자→스페이스LMN엔터) 유지
                             // scanf 실행:  문자 1개 스페이스 입력/저장 X 버퍼유지 O →  버퍼 변수(문자→스페이스) 삭제  →  버퍼 변수(문자→LMN엔터) 유지
                             // scanf 실행:  입력서식(%s→문자'열') 버퍼 변수자료형(문자→LMN엔터) 매칭 O
                             // scanf 실행 성공:  I 주소값(&)에 문자 3개 LMN 입력/저장(공백문자 전까지) + 문자 1개 \0=null 생성   →  버퍼 변수(문자→엔터) 유지  
    printf("---Output---\n"); 
    printf("<%s>\n", I);     // %s → 문자 3개 LMN 출력
    printf("<%c> <%c> <%c>\n", I[0], I[1], I[2]); // %c → 선언한 자료형크기 만큼 문자 1개 L M N 각각 출력
    printf("<%c>\n", I[3] ); // \0=null 문자 생성 위치 

    printf("---Input---\n");
    scanf("%c", &I);         // scanf 실행:  입력서식(%c→문자) 버퍼 변수자료형(문자→엔터) 매칭 O
                             // scanf 실행 성공:  문자 1개 엔터 입력/저장[%c 문자자료형 엔터(1 Byte) I 주소값(&)에 입력/저장]   
    printf("---Output---\n"); 
    printf("<%c>\n", I[0]);  // %c → 선언한 자료형크기 만큼 문자 1개 엔터 출력
    printf("<%c>\n<%c> <%c>\n", I[0], I[1], I[2]); // %c → 선언한 자료형크기 만큼 문자 3개 엔터 M N 각각 출력
    printf("<%s>\n", I);
   
    return 0;
}


```

![](https://t9003081320.p.clickup-attachments.com/t9003081320/74ee236d-cb9b-43a2-a80e-4a2770ad0504/Picture12.png)

**f\_char\_array\_4.c**

```cpp
// 공백문자 O  /  문자 → %s 
// 예:  스페이스스페이스ABCD스페이스EFG 입력/저장

#include <stdio.h>

int main()
{
    char I;

    printf("---Input---\n");
    scanf("%s", &I);         // 입력서식(%s→문자'열') '초기화' 변수자료형(문자→스페이스스페이스ABCD스페이스EFG엔터) 매칭 O  →  공백문자 예외문자 Set UP 
                             // scanf 실행 대기:  문자 1개 스페이스 입력/저장 X 버퍼유지 O  →  버퍼 변수(문자→스페이스) 삭제  →  '초기화' 변수(문자→스페이스ABCD스페이스EFG엔터) 버퍼 유지 
                             // scanf 실행 대기:  문자 1개 스페이스 입력/저장 X 버퍼유지 O  →  버퍼 변수(문자→스페이스) 삭제  →  버퍼 변수(문자→ABCD스페이스EFG엔터) 유지
                             // scanf 실행: 입력서식(%s→문자'열') 버퍼 변수자료형(문자'열'→ABCD스페이스스페이스CD엔터) 매칭 O
                             // scanf 실행 성공:  char I 메모리박스 '활성화' 문자 1개 A 입력/저장 + char I 메모리박스 '생성' 문자 4개 BCD 입력/저장 (공백문자 전까지) + \0=null 문자 생성  →  버퍼 변수(문자→스페이스EFG엔터)유지
    printf("---Output---\n"); 
    printf("<%c>\n", I);     // %c → 선언한 자료형크기 만큼 문자 1개 A 출력:  %s → 컴파일링 오류
    
    printf("---Input---\n");
    scanf("%s", &I);         // 입력서식(%s→문자'열') 버퍼 변수자료형(문자→스페이스EFG엔터) 매칭 O
                             // scanf 실행 대기:  문자 1개 스페이스 입력/저장 X 버퍼유지 O  →  버퍼 변수(문자→스페이스) 삭제  →  '초기화' 변수(문자→EFG엔터) 버퍼 유지 
                             // scanf 실행: 입력서식(%s→문자'열') 버퍼 변수자료형(문자'열'→EFG엔터) 매칭 O
                             // scanf 실행 성공:  char I 메모리박스 '활성화' 문자 1개 E 입력/저장 + char I 메모리박스 '생성' 문자 3개 FG 입력/저장 (공백문자 전까지) + \0=null 문자 생성  →  버퍼 변수(문자→엔터)유지
    printf("---Output---\n"); 
    printf("<%c>\n", I);     // %c → 선언한 자료형크기 만큼 문자 1개 E 출력:  %s → 컴파일링 오류

    printf("---Input---\n");
    scanf("%s", &I);         // 입력서식(%s→문자'열') 버퍼 변수자료형(문자→엔터) 매칭 O  →  공백문자 예외문자 Set UP 
                             // scanf 실행 대기:  문자 1개 엔터 입력/저장 X 버퍼유지 O  →  버퍼 변수(문자→엔터) 삭제  
                             // scanf 실행: 입력대기
    return 0;
}
```

![](https://t9003081320.p.clickup-attachments.com/t9003081320/0b9a8d35-1e63-4923-912e-c83f4df7b20c/Picture13.png)

**f\_char\_array\_5.c**

```cpp
// 배열크기 O  /  공백문자 O  /  문자'열' → scanf %c
// 예:  스페이스스페이스AB 입력/저장

#include <stdio.h>

int main()
{
    char I[10] = {0};       // char I[10]; 메모리박스 10개 '활성화'

    printf("---Input---\n");
    scanf("%c", &I);        // 입력서식(%c→문자) '초기화' 변수자료형(문자→스페이스스페이스AB엔터 입력/저장) 매칭 O
                            // scanf 실행 성공:  문자 1개 스페이스 입력/저장[%c 문자자료형 스페이스(1 Byte) I 주소값(&)에 입력/저장]  →  '초기화' 변수(문자→스페이스AB엔터) 버퍼 유지
    printf("---Output---\n");
    printf("<%s>\n", I);    // %s → 문자 스페이스 출력
    printf("<%c>\n", I[0]); // %c → 선언한 자료형크기 만큼 문자 1개 스페이스 출력
    printf("<%c>\n", I[1]); // 초기화 \0=null 문자 

    printf("---Input---\n");
    scanf("%c", &I);        // 입력서식(%c→문자) '초기화' 변수자료형(문자→스페이스AB엔터) 매칭 O
                            // scanf 실행 성공:  문자 1개 스페이스 입력/저장[%c 문자자료형 스페이스(1 Byte) I 주소값(&)에 입력/저장]  →  '초기화' 변수(문자→AB엔터) 버퍼 유지
    printf("---Output---\n");
    printf("<%s>\n", I);    // %s → 문자 스페이스 출력
    printf("<%c>\n", I[0]); // %c → 선언한 자료형크기 만큼 문자 1개 스페이스 출력
    printf("<%c>\n", I[1]); // 초기화 \0=null 문자 

    printf("---Input---\n");
    scanf("%c", &I);        // 입력서식(%c→문자) '초기화' 변수자료형(문자→AB엔터) 매칭 O
                            // scanf 실행 성공:  문자 1개 A 입력/저장[%c 문자자료형 A(1 Byte) I 주소값(&)에 입력/저장]  →  '초기화' 변수(문자→B엔터) 버퍼 유지
    printf("---Output---\n");
    printf("<%s>\n", I);    // %s → 문자 A 출력
    printf("<%c>\n", I[0]); // %c → 선언한 자료형크기 만큼 문자 1개 A 출력
    printf("<%c>\n", I[1]); // 초기화 \0=null 문자

    printf("---Input---\n");
    scanf("%c", &I);        // 입력서식(%c→문자) '초기화' 변수자료형(문자→B엔터) 매칭 O
                            // scanf 실행 성공:  문자 1개 B 입력/저장[%c 문자자료형 B(1 Byte) I 주소값(&)에 입력/저장]  →  '초기화' 변수(문자→엔터) 버퍼 유지
    printf("---Output---\n");
    printf("<%s>\n", I);    // %s → 문자 B 출력
    printf("<%c>\n", I[0]); // %c → 선언한 자료형크기 만큼 문자 1개 B 출력
    printf("<%c>\n", I[1]); // 초기화 \0=null 문자

    printf("---Input---\n");
    scanf("%c", &I);        // 입력서식(%c→문자) '초기화' 변수자료형(문자→엔터) 매칭 O
                            // scanf 실행 성공:  문자 1개 엔터 입력/저장[%c 문자자료형 엔터(1 Byte) I 주소값(&)에 입력/저장]
    printf("---Output---\n");
    printf("<%s>\n", I);    // %s → 문자 엔터 출력
    printf("<%c>\n", I[0]); // %c → 선언한 자료형크기 만큼 문자 1개 엔터 출력
    printf("<%c>\n", I[1]); // 초기화 \0=null 문자 

    return 0;
}


```

![](https://t9003081320.p.clickup-attachments.com/t9003081320/a181c228-4ab0-41a1-9d82-21ffa15cf401/Picture14.png)

**f\_char\_array\_6.c**

```cpp
// 배열크기 X  /  공백문자 X  /  문자 → %c  문자'열 → %s
// 예: 12345 입력/저장 → 123456 입력/저장

#include <stdio.h>

int main()
{
    char A[2];
    printf("---Input---\n");
    scanf("%s", &A);          // 입력서식(%s→문자'열') '초기화' 변수자료형(문자→12345엔터) 매칭 O
                              // scanf 실행 성공:  A 주소값(&)에 문자 5개 12345 입력/저장(공백문자 전까지) + 문자 1개 \0=null 생성(char A[2]; 메모리박스 2개 '활성화' + 4개 '생성')  →  '초기화' 변수(문자→엔터) 버퍼 유지 
    printf("---Output---\n");
    printf("<%s>\n", A);      // %s → 문자 5개 12345 출력

    char B;
    printf("---Input---\n");
    scanf("%c", &B);          // 입력서식(%c→문자) 버퍼 변수자료형(문자→엔터) 매칭 O
                              // scanf 실행 성공:  문자 1개 엔터 입력/저장[%c 문자자료형 엔터(1 Byte) C 주소값(&)에 입력/저장]  
    printf("---Output---\n");
    printf("<%c>\n", B);      // %c → 선언한 자료형크기 만큼 문자 1개 엔터 출력

    char C;
    printf("---Input---\n");
    scanf("%c", &C);          // 입력서식(%c→문자) '초기화' 변수자료형(문자→123456엔터) 매칭 O
                              // scanf 실행 성공:  문자 1개 1 입력/저장[%c 문자자료형 1(1 Byte) C 주소값(&)에 입력/저장]  →  '초기화' 변수(문자→23456엔터) 버퍼 유지 
    printf("---Output---\n");
    printf("<%c>\n", C);      // %c →  선언한 자료형크기 만큼 문자 1개 1 출력

    char D[5];
    printf("---Input---\n");
    scanf("%s", &D);          // 입력서식(%s→문자'열') 버퍼 변수자료형(문자→23456엔터) 매칭 O
                              // scanf 실행 성공:  D 주소값(&)에 문자 5개 23456 입력/저장(공백문자 전까지) + 문자 1개 \0=null 생성(char A[5]; 메모리박스 5개 '활성화')  →  '초기화' 변수(문자→엔터) 버퍼 유지 
    printf("---Output---\n");
    printf("<%s>\n", D);      // %s → 문자 5개 23456 출력
    printf("<%c>\n", C);      // %c → 선언한 자료형크기 만큼 문자 1개 \0=null 출력
    printf("<%c>\n", B);      // %c → 선언한 자료형크기 만큼 문자 1개 엔터 출력
    printf("<%s>\n", A);      // %s → 문자 5개 12345 출력       


    return 0;
}


```

![](https://t9003081320.p.clickup-attachments.com/t9003081320/9939365e-7ed8-4380-ac2d-b1f00c154cee/Picture15.png)

**f\_char\_array\_7.c**

```cpp
// 배열크기 X  /  공백문자 X  /  문자 → %c  문자'열 → %s
// 예: 12345 입력/저장

#include <stdio.h>

int main()
{
    char A;
    printf("---Input---\n");
    scanf("%s", &A);          // 입력서식(%s→문자'열') '초기화' 변수자료형(문자→12345엔터) 매칭 O
                              // scanf 실행 성공:  char A 메모리박스 '활성화' 문자 1개 1 입력/저장 + char A 메모리박스 '생성' 문자 5개 2345 입력/저장 + \0=null 문자 생성  →  버퍼 변수(문자→엔터)유지
    printf("---Output---\n");
    printf("<%c>\n", A);      // %c → 선언한 자료형크기 만큼 문자 1개 1 출력

    char B[3];
    printf("---Input---\n");
    scanf("%s", &B);          // 입력서식(%s→문자'열') 버퍼 변수자료형(문자→엔터) 매칭 O  →  공백문자 예외문자 Set UP 
                              // scanf 실행 대기:  문자 1개 엔터 입력/저장 X 버퍼유지 O  →  버퍼 변수(문자→엔터) 삭제 
                              // scanf 실행:  '초기화' 변수자료형(문자→12345엔터) 매칭 O
                              // scanf 실행 성공:  B 주소값(&)에 문자 5개 12345 입력/저장(공백문자 전까지) + 문자 1개 \0=null 생성  →  버퍼 변수(문자→엔터) 유지  
    printf("---Output---\n");
    printf("<%s>\n", B);      // %s → 문자 5개 12345 출력    

    char C;
    printf("---Input---\n");
    scanf("%s", &C);          // 입력서식(%s→문자'열') 버퍼 변수자료형(문자→엔터) 매칭 O  →  공백문자 예외문자 Set UP 
                              // scanf 실행 대기:  문자 1개 엔터 입력/저장 X 버퍼유지 O   →  버퍼 변수(문자→엔터) 삭제 
                              // scanf 실행:  '초기화' 변수자료형(문자→12345엔터) 매칭 O
                              // scanf 실행 성공:  char C 메모리박스 '초기화' 문자 1개 1 + char B 메모리박스 위치 '초기화' 문자 3개 234 + char A 메모리박스 위치 '초기화' 문자 1개 5 (공백문자 전까지) + 문자 1개 \0=null 생성  →  '초기화' 변수(문자→엔터) 버퍼 유지
    printf("---Output---\n");
    printf("<%c>\n", C);      // %c → 선언한 자료형크기 만큼 문자 1개 1 출력

    char D[3] = {0};
    printf("---Input---\n");
    scanf("%c", &D);          // 입력서식(%c→문자) 버퍼 변수자료형(문자→엔터) 매칭 O 
                              // scanf 실행 성공:  문자 1개 1 엔터 입력/저장[%c 문자자료형 엔터(1 Byte) C 주소값(&)에 입력/저장]  
    printf("---Output---\n");
    printf("<%s>\n", D);      // %s → 문자 엔터 출력

    char E[3] = {0};
    printf("---Input---\n");
    scanf("%c", &E);          // 입력서식(%s→문자'열') 버퍼 변수자료형(문자→12345엔터) 매칭 O
                              // scanf 실행 성공:  문자 1개 1 입력/저장  →  '초기화' 변수(문자→2345엔터) 버퍼 유지 
    printf("---Output---\n");
    printf("<%s>\n", E);      // %s → 문자 1개 1 출력
    printf("<%s>\n", D);      // %s → 문자 1개 엔터 출력
    printf("<%c>\n", C);      // %c → 선언한 자료형크기 만큼 문자 1개 1 출력
    printf("<%s>\n", B);      // %s → 문자 5개 12345 출력
    printf("<%c>\n", A);      // %c → 선언한 자료형크기 만큼 문자 1개 5 출력       

    return 0;
}


```

![](https://t9003081320.p.clickup-attachments.com/t9003081320/50886707-92b7-4f8d-bb3a-cea88b48104f/Picture16.png)

**f\_char\_array\_8.c**

```cpp
// 배열크기 X  /  공백문자 O  /  문자 → %c  문자'열 → %s
// 예: 1스페이스2스페이스345

#include <stdio.h>

int main()
{
    char A;
    printf("---Input---\n");
    scanf("%c", &A);          // 입력서식(%c→문자) 버퍼 변수자료형(문자→1스페이스2스페이스3엔터) 매칭 O 
                              // scanf 실행 성공:  문자 1개 A 입력/저장[%c 문자자료형 A(1 Byte) A 주소값(&)에 입력/저장]  →  '초기화' 변수(문자→스페이스2스페이스345) 버퍼 유지  
    printf("---Output---\n"); 
    printf("<%c>\n", A);      // %c → 선언한 자료형크기 만큼 문자 1개 1 출력

    char B[3];
    scanf("%s", &B);          // 입력서식(%s→문자'열') 버퍼 변수자료형(문자→스페이스2스페이스345엔터) 매칭 O → 공백문자 예외문자 Set UP 
                              // scanf 실행 대기:  문자 1개 스페이스 입력/저장 X 버퍼유지 O  →  버퍼 변수(문자→스페이스) 삭제 
                              // scanf 실행:  버퍼 변수자료형(문자→2스페이스345엔터) 매칭 O
                              // scanf 실행 성공:  B 주소값(&)에 문자 1개 2 입력/저장(공백문자 전까지) + 문자 1개 \0=null 생성  →  버퍼 변수(문자→스페이스345) 유지  
    printf("---Output---\n");
    printf("<%s>\n", B);      // %s → 문자 2 출력

    char C;
    printf("---Input---\n");
    scanf("%c", &C);          // 입력서식(%c→문자) 버퍼 변수자료형(문자→스페이스345엔터) 매칭 O 
                              // scanf 실행 성공:  문자 1개 스페이스 입력/저장[%c 문자자료형 스페이스(1 Byte) C 주소값(&)에 입력/저장]  →  버퍼 변수(문자→345엔터) 유지  
    printf("---Output---\n"); 
    printf("<%c>\n", C);      // %c → 선언한 자료형크기 만큼 문자 1개 스페이스 출력

    char D[3];
    scanf("%s", &D);          // scanf 실행:  버퍼 변수자료형(문자→345엔터) 매칭 O
                              // scanf 실행 성공:  D 주소값(&)에 문자 3개 345 입력/저장(공백문자 전까지) + 문자 1개 \0=null 생성  →  버퍼 변수(문자→엔터) 유지  
    printf("---Output---\n");
    printf("<%s>\n", D);      // %s → 문자 3개 345 출력

    char E;
    printf("---Input---\n");
    scanf("%c", &E);          // 입력서식(%c→문자) 버퍼 변수자료형(문자→엔터) 매칭 O 
                              // scanf 실행 성공:  문자 1개 엔터 입력/저장[%c 문자자료형 엔터(1 Byte) E 주소값(&)에 입력/저장]
    printf("---Output---\n"); 
    printf("<%c>\n", E);      // %c → 선언한 자료형크기 만큼 문자 1개 엔터 출력
    printf("<%s>\n", D);      // %s → 문자 3개 345 출력
    printf("<%c>\n", C);      // %c → 선언한 자료형크기 만큼 문자 1개 \0=null 출력
    printf("<%s>\n", B);      // %s → 문자 1개 2 출력
    printf("<%c>\n", A);      // %c → 선언한 자료형크기 만큼 문자 1개 1 출력

    return 0;
}


```

![](https://t9003081320.p.clickup-attachments.com/t9003081320/6528ab5a-c2e2-4a5b-9a31-2e49a905a0c9/Picture17.png)

**f\_char\_array\_9.c**

```cpp
// 배열크기 X  /  공백문자 O  /  문자 → %s  문자'열 → %c
// 예: 1스페이스2스페이스345 

#include <stdio.h>

int main()
{
    char A[3] = {0};
    printf("---Input---\n");
    scanf("%c", &A);          // 입력서식(%c→문자) '초기화' 변수자료형(문자→1스페이스2스페이스345엔터) 매칭 O 
                              // scanf 실행 성공:  문자 1개 1 입력/저장[%c 문자자료형 1(1 Byte) A 주소값(&)에 입력/저장]  →  '초기화' 변수(문자→스페이스2스페이스345엔터) 버퍼 유지  
    printf("---Output---\n"); 
    printf("<%s>\n", A);      // %c → 선언한 자료형크기 만큼 문자 1개 A 출력

    char B;
    printf("---Input---\n");
    scanf("%c", &B);          // 입력서식(%c→문자) 버퍼 변수자료형(문자→스페이스2스페이스345엔터) 매칭 O 
                              // scanf 실행 성공:  문자 1개 스페이스 입력/저장[%c 문자자료형 스페이스(1 Byte) B 주소값(&)에 입력/저장]  →  버퍼 변수(문자→2스페이스345엔터) 버퍼 유지  
    printf("---Output---\n"); 
    printf("<%c>\n", B);      // %c → 선언한 자료형크기 만큼 문자 1개 스페이스 출력

    char C;
    printf("---Input---\n");
    scanf("%c", &C);          // 입력서식(%c→문자) 버퍼 변수자료형(문자→2스페이스345엔터) 매칭 O 
                              // scanf 실행 성공:  문자 1개 2 입력/저장[%c 문자자료형 2(1 Byte) C 주소값(&)에 입력/저장]  →  버퍼 변수(문자→스페이스345엔터) 버퍼 유지  
    printf("---Output---\n"); 
    printf("<%c>\n", C);      // %c → 선언한 자료형크기 만큼 문자 1개 2 출력

    char D[3] = {0};
    printf("---Input---\n");
    scanf("%c", &D);          // 입력서식(%c→문자) 버퍼 변수자료형(문자→스페이스345엔터) 매칭 O 
                              // scanf 실행 성공:  문자 1개 스페이스 입력/저장[%c 문자자료형 스페이스(1 Byte) D 주소값(&)에 입력/저장]  →  버퍼 변수(문자→345엔터) 버퍼 유지  
    printf("---Output---\n"); 
    printf("<%s>\n", D);      // %c → 선언한 자료형크기 만큼 문자 1개 스페이스 출력
 
    char E;
    printf("---Input---\n");
    scanf("%s", &E);          // 입력서식(%s→문자'열') 버퍼 변수자료형(문자→345엔터) 매칭 O 
                              // scanf 실행 성공:  char E 메모리박스 '초기화' 문자 1개 3 + char D 메모리박스 위치 '초기화'문자 2개 45 (공백문자 전까지) + 문자 1개 \0=null 생성  → '초기화' 변수(문자→엔터) 버퍼 유지
    printf("---Output---\n"); 
    printf("<%c>\n", E);      // %c → 선언한 자료형크기 만큼 문자 1개 3 출력
    printf("<%s>\n", D);      // %s → 문자 2개 45 출력: \0=null 문자 앞까지
    printf("<%c>\n", C);      // %c → 선언한 자료형크기 만큼 문자 1개 2 출력
    printf("<%c>\n", B);      // %c → 선언한 자료형크기 만큼 문자 1개 스페이스 출력
    printf("<%s>\n", A);      // %c → 선언한 자료형크기 만큼 문자 1개 A 출력      

    return 0;
}


```

![](https://t9003081320.p.clickup-attachments.com/t9003081320/7da86d42-440e-496d-92d2-4c69dcc8cf06/Picture18.png)